# 토킹 튜터 — 작업 지침

폰만으로 만드는 영어 회화 연습 봇. 이 파일은 클로드가 세션을 시작할 때 자동으로 읽는 **행동 지침**이다.
무엇을 만드는지(밑그림)와 언제 만드는지(계획)는 `docs/` 에 따로 있다.

---

## 세션을 시작하면

`.claude/hooks/session-start.sh` 가 **자동으로** `docs/plan.md` 의 현재 상태와 지금 차례인 Day 를 읽어 맥락에 넣는다. 그 브리핑을 먼저 확인한다.

브리핑이 보이지 않으면(훅이 실패했으면) 직접 읽는다.

1. `docs/plan.md` 맨 위의 **현재 상태** — 지금 며칠째이고 무엇을 할 차례인지
2. `docs/decisions.md` — 이미 정해진 것을 다시 논의하지 않기 위해
3. 무엇을 만드는지가 헷갈리면 `docs/overview.md`
4. 여기까지 어떻게 왔는지가 궁금하면 `docs/history/`

이 문서들이 이전 세션의 기억을 대신한다. 컨테이너는 사라지고 문서만 남는다.

---

## 사용자에 대해

- **이런 방식의 개발이 처음이다.** 전문 용어를 쓰면 그 자리에서 풀어서 설명한다. 나중에 몰아서 설명하지 않는다.
- **폰(안드로이드)과 아이패드만 쓴다.** 데스크톱이 없다. 터미널을 열 수 없다.
- **만드는 과정을 이해하는 것 자체가 목적이다.** 빠른 길과 이해되는 길이 갈리면 **후자로 간다.** 혼자 5분에 끝낼 수 있는 일도 왜 그렇게 하는지 말하면서 한다.
- 한국어로 대화한다.
- 한 번에 하나씩 짚고 넘어가는 것을 선호한다. 여러 단계를 한꺼번에 밀어붙이지 않는다.

---

## 절대 하지 않는다

- API 키나 비밀값을 코드·커밋·채팅에 넣기. **환경변수에만 둔다.** 실수로 올라가면 지우지 말고 **폐기 후 재발급**을 안내한다.
- 사용자에게 터미널 명령을 시키기. 폰에는 터미널이 없다. 웹 화면에서 **어디를 누르는지**로 안내한다.
- 여러 날치 작업을 한 번에 몰아서 하기. 하루 분량은 20~25분이다.
- 계획에 없는 기능을 임의로 추가하기. 필요해 보이면 먼저 제안하고 `decisions.md` 에 남긴다.
- 합격 판정을 건너뛰고 다음 주차로 넘어가기.

---

## 항상 한다

- 코드를 만들면 **그날 안에 푸시한다.** 컨테이너가 사라지면 푸시하지 않은 것은 전부 사라진다.
- 작업이 끝나면 `docs/plan.md` 의 **현재 상태**를 갱신한다.
- 새로 정해진 것이 있으면 `docs/decisions.md` 맨 위에 날짜와 함께 추가한다.
- 사실 확인이 필요한 수치(무료 한도, 요금)는 추측하지 않는다. 확인할 수 없으면 "가입 화면에서 확인" 으로 남긴다.
- 사용자가 직접 해야 하는 일과 내가 하는 일을 매번 구분해서 말한다.

---

## 확정된 기술 결정

바꾸려면 사용자 동의가 필요하다. 자세한 이유는 `docs/decisions.md`.

| 항목 | 선택 |
|---|---|
| 호스팅 | Vercel (깃허브 푸시 → 자동 배포) |
| 대화 모델 | Gemini 3.6 Flash (`GEMINI_MODEL` 환경변수로 교체 가능) |
| 아바타 | 브라우저가 실시간으로 그리는 캐릭터 (립싱크 영상 아님) |
| 음성 인식 | 브라우저 내장 + 먹통 감지 |
| 기준 기기 | 안드로이드 크롬 |

**아이패드 주의** — 아이패드는 **사파리로만** 음성 인식이 된다. 크롬 등 다른 앱에서는 기능이 있는 것처럼 보이면서 조용히 실패한다. 그래서 기능 검사가 아니라 **몇 초간 반응이 오는지**로 판단해 타이핑 안내로 넘긴다.

---

## 기록 규칙

세 가지 문서가 각각 다른 주기로 갱신된다. 섞지 않는다.

| 문서 | 언제 | 무엇을 |
|---|---|---|
| `docs/plan.md` | **작업이 끝날 때마다** | 현재 상태 표, 항목의 상태 기호 `[ ] [~] [x] [-]` |
| `docs/decisions.md` | **새로 정해진 것이 생길 때** | 무엇을 / 왜 / 무엇이 생기면 다시 볼까. 맨 위에 날짜와 함께 추가 |
| `docs/history/NN-*.md` | **작업 덩어리가 끝날 때** (대략 주차 단위) | 실제로 어떻게 진행됐는지. `TEMPLATE.md` 서식 |

**분기점 기록은 주차 합격 판정을 통과한 직후에 쓴다.** Day 4 · 8 · 12 · 16 이 그 시점이다.
미리 정해둔 파일명 — `01-skeleton` · `02-voice` · `03-tutor` · `04-avatar`

기록에서 **잘 안 된 것을 빼지 않는다.** 막혔던 것과 어떻게 풀었는지가 가장 쓸모 있는 기록이다.
한 번 쓴 분기점 기록은 고치지 않는다. 생각이 바뀌면 다음 기록에 쓴다.

계획을 바꿀 때는 `plan.md` 의 해당 줄을 **지우지 않고** 취소선과 이유를 남긴다.

---

## 명령어

| 명령 | 하는 일 |
|---|---|
| `/today` | 오늘의 할 일을 `docs/plan.md` 에서 불러온다 |
| `/milestone` | 방금 끝낸 작업 덩어리를 `docs/history/` 에 기록하고 푸시한다 |

사용자가 "오늘 뭐 하지", "오늘의 할 일" 처럼 물으면 `/today` 를 쓰지 않았어도 같은 내용을 답한다.

---

## 저장소 규칙

- 작업·푸시 브랜치: `claude/english-video-tutor-bot-if7735`
- 이 브랜치가 대표 브랜치이고, Vercel 이 이것을 배포한다.
- 커밋 메시지는 한국어로, 무엇을 왜 바꿨는지 한 줄.
